<f:RibbonWindow x:Class="LogAnalyzer.Windows.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:local="clr-namespace:LogAnalyzer"
                xmlns:c="clr-namespace:LogAnalyzer.Wpf.Converters;assembly=LogAnalyzer.Wpf"
                     xmlns:f="urn:fluent-ribbon"
                     mc:Ignorable="d"
                     Title="MainWindow" Height="350" Width="525">
    <f:RibbonWindow.Resources>
        <c:ColorToBrushConverter x:Key="ColorToBrushConverter" />
    </f:RibbonWindow.Resources>
    <DockPanel>
        <f:Ribbon DockPanel.Dock="Top">
            <f:Ribbon.Menu>
                <f:Backstage>
                    <f:BackstageTabControl>
                        <f:BackstageTabItem Header="Open" />
                    </f:BackstageTabControl>
                </f:Backstage>
            </f:Ribbon.Menu>
            <f:RibbonTabItem Header="Home">
                <f:RibbonGroupBox Header="File">
                    <f:Button Header="Open" 
                              LargeIcon="/Resources/Images/Open32.png" 
                              Icon="/Resources/Images/Open16.png" 
                              Command="{Binding OpenCommand}"/>
                </f:RibbonGroupBox>
            </f:RibbonTabItem>
        </f:Ribbon>
        <ListView ItemsSource="{Binding LogEntries}" SelectionMode="Extended">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="" DisplayMemberBinding="{Binding LogEntry.LogEntryIndex}" />
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding LogEntry.Date}" />
                    <GridViewColumn Header="Verbosity" DisplayMemberBinding="{Binding LogEntry.Verbosity}" />
                    <GridViewColumn Header="Message">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LogEntry.Message}" 
                                           Foreground="{Binding Highlight.Foreground, Converter={StaticResource ColorToBrushConverter}}" 
                                           Background="{Binding Highlight.Background, Converter={StaticResource ColorToBrushConverter}}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</f:RibbonWindow>
